import reg from '../assets/regression-krigging-for-geodata.jpg';

import reg1 from '../assets/regression-krigging-for-geodata1.jpg';

const ImageReg = [
	{
		image: `${reg}`,
	},
	{
		image: `${reg1}`,
	},
];

export default ImageReg;
